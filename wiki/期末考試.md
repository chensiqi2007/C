# 期末考試
## 代碼題
### 0 代碼
    #include<stdio.h>
    
    int mysum(int n) {
        int sum = 0 , i = 0;
        for (i = 1; i <= n; i++) {
          sum=sum+i;
        }
    return sum;
    }
    
    int main(){
      int n = 0 , y = 0;
      printf("Input data: ");
      scanf("%d", &n);
      y=mysum(n);
      printf("1+2+3+···+%d= %d\n",n, y);
      return 0;
    }


### 1 代碼
    #include<stdio.h>
    #define N 3

    int mysum(int len , int a[]) {
        int sum=0;
        for(int i=1;i<=len;i++)
            sum+=a[i];
        return sum;
    }
    
    int main (void) {
        int a[N];
        for(int i=0;i<N;i++) {
            printf("Please input the %dth number: ",i+1);
            scanf("%d",&a[i]);
        }
    
        for (int i=0;i<N;i++) {
            printf("%5d ",a[i]);
        }
        printf("\n");
        printf("The sum of the array is %d\n",mysum(N,a));
        return 0;
    }


### 2 代碼
    #include <stdio.h>
    #define N 3
    
    void creat_array (int a[], int n ) {
        for (int i=0; i<N ; i++) {
            printf("請輸入數組：",i++);
            scanf("%d", &a[i]);
        }
    }
    
    int get_sum (int a[] , int n) {
        int sum = 0;
        for (int i; i<N ; i++) {
            sum += a[i];
        }
        return sum;
    }
    
    void display (int a[] , int n) {
        for (int i =0; i<N ; i++) {
            printf("%5d", a[i]);
        }
    }
    
    int main (void) {
        int a[N];
        creat_array(a,N);
        display(a,N);
        printf("\n");
        printf("和為：%d\n", get_sum(a,N));\
        return 0;
    }


### 3 代碼
    #include<stdio.h>
    #define N 3
    
    struct  student
    {
        char name[20];
        char id[9];
        float score;
    };
    
    void creat_student_array (struct student a[] , int len)
    {
        for (int i = 0; i < len ; i++)
        {
            (void)printf("請輸入第%d個學生的學號：\n",i+1);
            (void)scanf("%s",a[i].name);
        
            (void)printf("請輸入第%d個學生的姓名：\n",i+1);
            (void)scanf("%s",a[i].id);
            
            (void)printf("請輸入第%d個學生的分數：\n",i+1);
            (void)scanf("%f",&a[i].score);
        }
    }
    
    void print_student_array (struct student a[] , int len)
    {
        for (int i = 0; i < len ; i++)
        {
            (void)printf("%-10s,%-10s,%2f\n",a[i].id,a[i].name,a[i].score);
            (void)printf("\n ==================== \n");
        }
    }
    
    void bubble_student_array (struct student a[] , int len)
    {
        for (int i=0 ; i < len-1 ; i++)
        {
            for (int j=0 ; j < len-1-i ; j++)
            {
                if (a[j].score > a[j+1].score)
                {
                    struct student temp = a[j];
                    a[j] = a[j+1];
                    a[j+1] = temp;
                }
            }
        }
    }
    
    void select_student_array (struct student a[] , int len)
    {
        for (int i=0 ; i < len-1 ; i++)
        {
            int index = i;
            for (int j=i+1 ; j < len ; j++)
            {
                if (a[index].score < a[j].score)
                {
                    index = j;
                }
            }
            if (i!= index)
            {
                struct student temp = a[i];
                a[i] = a[index];
                a[index] = temp;
            }
        }
    }
    
    int main (void)
    {
        struct student a[N];
        creat_student_array(a,N);
        print_student_array(a,N);
        (void)printf("\n 冒泡排序 正序 \n ==================== \n");
        bubble_student_array(a,N);
        print_student_array(a,N);
        (void)printf("\n 選擇排序 倒序 \n==================== \n");
        select_student_array(a,N);
        print_student_array(a,N);
        return 0;
    }
    
