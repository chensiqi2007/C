# 基礎

# 進階
## 素數的檢測
### 代碼
    #include <stdio.h>
    #include <stdlib.h>
    int main (void) {
      (void)system("chcp 65001");
      int a;
      (void)printf("請輸入一個數: ");
      (void)scanf("%d", &a);
      int flag = 1;
      if (a == 0 || a == 1) {
          (void)printf("不合法");
          return 1;
      }
      int i;
      for (i = 2; i < a; i++) {
          if (a % i == 0) {
              (void)printf("不是素數");
              return 1;
          }
      }
      (void)printf("是素數");
      return 0;
    }


